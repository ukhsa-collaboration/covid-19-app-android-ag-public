<?xml version="1.0" encoding="utf-8"?>
<merge xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/amber_improved"
    android:elevation="5dp"
    android:padding="@dimen/local_message_banner_padding"
    tools:parentTag="android.widget.LinearLayout">

    <FrameLayout
        android:layout_width="52dp"
        android:layout_height="match_parent">

        <ImageView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:contentDescription="@null"
            android:scaleType="centerInside"
            android:src="@drawable/ic_alert"
            android:tint="@color/colorOnAmber" />
    </FrameLayout>

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_marginEnd="16dp"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/bannerTitle"
            style="@style/Body1"
            android:textColor="@color/colorOnAmber"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            tools:text="A new variant of concern is in your area." />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:drawableEnd="@drawable/ic_chevron_right"
            android:drawableTint="#004299"
            android:gravity="center_vertical"
            android:includeFontPadding="false"
            android:paddingEnd="8dp"
            android:text="@string/local_message_banner_read_more"
            android:textColor="@color/primaryColorOnAmberImproved"
            android:textSize="16sp" />
    </LinearLayout>
</merge>